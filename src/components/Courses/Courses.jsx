import styles from '../Courses/Courses.module.css';

const Courses = () => {
  return (
    <ul className={styles.courseList}>
      <li className={styles.courseItem}></li>
      <li className={styles.courseItem}></li>
      <li className={styles.courseItem}></li>
    </ul>
  );
};

export default Courses;
